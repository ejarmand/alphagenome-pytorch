# How to Optimize

This directory is an agent workspace for brainstorming ways to implement and optimize model inference.

Each discussion may use its own subdirectory. Store every discussion comment as a Markdown file named with a UTC timestamp, for example `2026-08-15T19-30-00Z.md`. If a comment is a reply, add labels that connect it to the comment it replies to.
